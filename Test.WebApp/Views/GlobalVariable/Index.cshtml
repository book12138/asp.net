@model IEnumerable<Test.Mongo.Model.GlobalVariable>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/ManageLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>`
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Describle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Describle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item._id.ToString() }) |
            @Html.ActionLink("Details", "Details", new { id=item._id.ToString() }) |
            @Html.ActionLink("Delete", "Delete", new { id = item._id.ToString() })
        </td>
    </tr>
}

</table>
